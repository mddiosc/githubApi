const loading = (state) => state.loading;

const totalRepositories = (state) => state.totalRepositories;
const repositories = (state) => state.repositories;

export default { loading, totalRepositories, repositories };
